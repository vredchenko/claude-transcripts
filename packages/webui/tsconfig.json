{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // "bun" for the co-located *.test.ts specs (bun:test); the app code itself is
    // browser-only and uses nothing from it.
    "types": ["vite/client", "bun"],
    "jsx": "react-jsx",
    "lib": ["ESNext", "DOM", "DOM.Iterable"]
  },
  "include": ["src"]
}
