# GENERATED from the app model (@claude-transcripts/shared) by scripts/gen-k8s.ts.
# Do NOT edit by hand — run `bun run gen:k8s` to regenerate, and change the model
# (packages/shared/src/model/services.ts) instead. Inline edits here are lost silently.
# Overrides belong in a kustomize overlay — see deploy/k8s/README.md.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: claude-transcripts
  labels:
    app.kubernetes.io/name: app
    app.kubernetes.io/part-of: claude-transcripts
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: app
      app.kubernetes.io/part-of: claude-transcripts
  template:
    metadata:
      labels:
        app.kubernetes.io/name: app
        app.kubernetes.io/part-of: claude-transcripts
    spec:
      containers:
        - name: app
          image: ghcr.io/vredchenko/claude-transcripts-app:latest
          envFrom:
            - secretRef:
                name: claude-transcripts-env
          env:
            - name: WEBAPI_HOST
              value: 0.0.0.0
            - name: WEBAPI_PORT
              value: "7650"
            - name: COUCHDB_URL
              value: http://couchdb:5984
            - name: S3_ENDPOINT
              value: http://garage:3900
            - name: MEILI_HOST
              value: http://meilisearch:7700
          ports:
            - name: http
              containerPort: 7650
          readinessProbe:
            httpGet:
              path: /health
              port: 7650
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 6
---
apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: claude-transcripts
  labels:
    app.kubernetes.io/name: app
    app.kubernetes.io/part-of: claude-transcripts
spec:
  selector:
    app.kubernetes.io/name: app
    app.kubernetes.io/part-of: claude-transcripts
  ports:
    - name: http
      port: 7650
      targetPort: 7650
