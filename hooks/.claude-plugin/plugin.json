{
  "name": "claude-transcripts",
  "version": "0.0.16",
  "description": "Claude Transcripts — log every session to CouchDB + S3, show that it's recording, and read the history back.",
  "keywords": [
    "session-logging",
    "couchdb",
    "garage",
    "s3",
    "claude-code",
    "transcripts",
    "recall"
  ],
  "commands": "./commands/",
  "skills": "./skills/",
  "userConfig": {
    "recall_mode": {
      "type": "string",
      "title": "Recall mode",
      "description": "When Claude consults session history unprompted: off | suggest | auto (default: the instance's config, normally auto)",
      "required": false
    },
    "recall_scope": {
      "type": "string",
      "title": "Recall scope",
      "description": "Which sessions count: project (this directory) | host | all (default: project)",
      "required": false
    },
    "max_results": {
      "type": "string",
      "title": "Max results",
      "description": "Maximum search results a recall may use (default 5)",
      "required": false
    }
  }
}
