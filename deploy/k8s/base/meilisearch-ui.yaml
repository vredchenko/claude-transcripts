# GENERATED from the app model (@claude-transcripts/shared) by scripts/gen-k8s.ts.
# Do NOT edit by hand — run `bun run gen:k8s` to regenerate, and change the model
# (packages/shared/src/model/services.ts) instead. Inline edits here are lost silently.
# Overrides belong in a kustomize overlay — see deploy/k8s/README.md.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: meilisearch-ui
  namespace: claude-transcripts
  labels:
    app.kubernetes.io/name: meilisearch-ui
    app.kubernetes.io/part-of: claude-transcripts
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: meilisearch-ui
      app.kubernetes.io/part-of: claude-transcripts
  template:
    metadata:
      labels:
        app.kubernetes.io/name: meilisearch-ui
        app.kubernetes.io/part-of: claude-transcripts
    spec:
      containers:
        - name: meilisearch-ui
          image: riccoxie/meilisearch-ui:v0.14.1
          ports:
            - name: http
              containerPort: 24900
---
apiVersion: v1
kind: Service
metadata:
  name: meilisearch-ui
  namespace: claude-transcripts
  labels:
    app.kubernetes.io/name: meilisearch-ui
    app.kubernetes.io/part-of: claude-transcripts
spec:
  selector:
    app.kubernetes.io/name: meilisearch-ui
    app.kubernetes.io/part-of: claude-transcripts
  ports:
    - name: http
      port: 24900
      targetPort: 24900
